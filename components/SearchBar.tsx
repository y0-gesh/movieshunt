import React from 'react';
import { View, Text } from 'react-native';

const SearchBar = () => {
    return (
        <View>
            <Text>SEarch</Text>
        </View>
    );
};

export default SearchBar;